---
name: autojs-mobile-automation-yashu-public
description: 通过 AutoJS 在 Android 手机上自动执行任务：用户在电脑端描述需求，AI 规划步骤、生成或选择 AutoJS 脚本、下发手机执行并取回结果。激活条件：用户消息须包含以下关键词之一:`运行手机任务`、`AI控制手机`、`手机自动执行任务`、`手机自动化`、`autojs脚本`、`用autojs执行`、`帮我操作手机`、`下发手机任务`。
---

# 手机任务执行器（AI → AutoJS）

PC 端 AI 是"大脑"，手机端 AutoJS 是"双手"：用户一句话描述任务 → AI 拆成步骤链 → 逐步下发手机执行 → **全部做完或失败才汇报一次**。

> 用户侧教程 `<skill_dir>/使用示例.md`：用户问"能做什么/怎么用"时主动推荐。

## 不可违反的硬约束

1. **安全红线**：支付/转账/删除/授权类不可逆操作，执行前必须向用户确认一次；**一次一个 UI 任务**——手机屏幕同一时刻只能做一件事，UI 任务必须串行下发，严禁并发（中继侧同步短操作已互斥，并发返回 429）。
2. **路径硬规则**：分隔符一律 `/` 禁 `\`；node 参数禁用 `/c/...` 绝对路径；任务参数只写模板名、不拼 `tasks/...`（模板 TASK.md 里的 `--path tasks/...` 是等价写法，不必照抄）；但 **Read 模板正文必须用 `scripts/tasks/<name>/TASK.md` 完整路径**，写 `tasks/...` 会找不到文件；手机路径参数前加 `MSYS_NO_PATHCONV=1`；**先 `cd` 进技能目录再用相对路径调脚本**。（详解 `references/目录架构与路径约定.md`）
3. **回执语义**：`status:success` 只代表任务单跑完，**业务成败看 `result.ok`**。30 秒超时不是失败，任务仍在跑，用 `--status <taskId>` 续查；脚本静默崩溃时第一嫌疑是回执没走 `autojs_result` 广播（单纯 `console.log` 不回传）。
4. **结果交付**：**用户要看的交付物**（截图 PNG、导出文件）在终态汇报里**必须给出完整绝对路径**并用 `present_files` 呈现，不得只写文件名——产物常落在隐藏目录，用户只有绝对路径才能找到。仅用于**内部校验**的截图/OCR/控件结果**不必**推给用户，只汇报结论（如"已打开抖音"）。判断原则：这张图是给用户看的，还是给自己校验的？
5. **AI 读图能力按实测判断，勿武断**：先 `Read` 实测一次，能识别就据此决策，被过滤则改用 `ocr` / `inspect_control_*` / 问用户，不硬猜。
6. **UI 模式指令必须在脚本首行（重大坑）**：AutoJs6 只认**文件第一行**的 UI 模式指令来启用 UI 模式；引号写法**单引号 `'ui';` 与双引号 `"ui";` 均可**（2026-09-08 实测两种都支持）。中继下发必注入引导代码，老客户端会把它挤到第二行 → UI 模式静默失效（`activity` 未定义 / `ui.layout` 崩溃，整个脚本报废）。客户端已根治（2026-09-06：检测到 `ui` 指令开头自动提行），但**双保险仍在**：网页容器类需求一律用 `open-webview` 模板（启动器落盘 + `execScriptFile`，不依赖客户端版本）；自己写 UI 一次性脚本也用同样模式。**写 UI 脚本时指令必须是文件第 1 个字符，注释/说明一律放它后面**（顶部注释挡在前也会失效，详见 `references/AI_AutoJS编码强制规范.md` §2.0）。
7. **现场脚本全程 try-catch，错误必回传电脑**：任何经中继下发的脚本，主体逻辑必须包 try-catch，catch 里给 `result` 赋 `{ok:0, err}` 并靠 `events.on("exit")` 里 `autojs_result` 广播回执——**吞掉异常不回传 = PC 端只能干等超时**，是最难排查的静默失败。
8. **⛔ 永不把「客户端引擎」当停止目标**：手机端执行端本体（路线A = APK 打包入口 `main.js` / 路线B = `autojs-task-phone-client.js`）一停 = 手机与电脑**断开连接、悬浮球变红**，且 PC 侧无法远程唤醒，**必须用户手动在手机上重开 App**，历史事故高发点。据此：
   - 任务失败后要"清场"，**首选 `node scripts/run-task.js --stop <taskId>`** 按任务单精确强杀（客户端只停登记在册的那条引擎，不会退化为"停全部"）；
   - **禁止 `list-running-scripts` → 全量 stop 的野蛮清场**；只停**明确列出的业务脚本 id**；
   - `list-running-scripts` 回执里 `isClient:true` / `clientIds` 的条目**不是可停目标**，选 id 时必须跳过；
   - `stop-script-by-id` 已默认拒停客户端（返回 `ok:0` + `blockedClient`）——**看到这个拒绝不要绕过**，不许改传 `forceStopClient:true` 去强停。

## 授权提示（必须遵守，不可省略）

`run-task.js` / `run-project.js` / `deploy-project.js` / `pc-to-phone.js` 在**免费试用次数用完后会拒绝下发**。识别信号（命中任一即是）：

- **stdout** 出现 `========== 本技能需要授权（免费试用已用完）==========` 提示块；
- 命令退出码为 **5**。

此时你必须：

1. **立即停止**本次任务——不要重试，也不要换脚本/换接口绕（`/run`、`/run-project` 一律会被拒）；
2. 把提示块中的 **机器码**、**购买地址**、**授权文件** 三项**原样转达给用户**；
3. 明确告知：「把购买得到的授权码写入上面那个授权文件即可激活，立即生效，无需重启服务」。

**严禁**把这种情况总结成"接口不可用""网络异常""环境问题"，更**严禁**静默失败——用户看不到购买入口，等于白跑一趟。

> 退出码约定：`0` 成功 · `1` 参数/通用失败 · `2` 请求失败或手机未连接 · `3` 文件不存在 · `4` 环境不满足（手机端脚本根目录缺失） · `5` 需要授权。

## 执行 SOP

**依赖前提**：`cd scripts && npm install`

- **第 0 步 健康检查（中继存活 SOP）**：`curl -s http://localhost:9421/health` 判读——`"status":"ok"` 且 `"phone":"connected"` → 直接进第 1 步；`"phone":"disconnected"` → 按 `references/手机连接排障.md` 五步流程主动带用户连上，不许只甩一句"请连接手机"。**只有 health 返回「连接拒绝/无响应」时才启动中继**（**已在跑绝不重启**，会踢掉已连手机）——启动**必须用 Bash 工具的 `run_in_background:true` 受管后台任务，严禁命令末尾 `&`**（`&` 会在本轮对话结束时被环境回收，下一轮手机就超时连不上）。完整启动/验证/判别见 `references/中继存活SOP.md`。
- **第 1 步 拆解（两级读取）**：先 `node scripts/scan-tasks.js` 拿全部模板 name+description 挑模板，选中后**才** Read 该 `scripts/tasks/<name>/TASK.md` 全文（description 即选择依据，正文即决策契约）。模板覆盖不了的才现场写脚本到 `temp/`，**写前必读** `references/AI_AutoJS编码强制规范.md` 与 `references/现场脚本规范.md`；凡调用 `captureScreen()`/`ocr()` 的脚本**必须内置授权弹框前置代码**，否则截图失败或脚本卡死（见 `references/截图权限与弹框处理.md`）。
- **第 2 步 逐步下发**：`cd <skill_dir> && node scripts/run-task.js <模板名> --args '{...}'`
  - `ok:1` → 下一步；`ok:0` → 换策略重试一次（如 tap-text 找不到就截图看屏），仍失败则终止并汇报卡点；
  - **长短任务判定**：原子操作（点/滑/截屏/开 App，<30 秒）→ 短，走默认同步等待；耗时随规模变或不可预估 → 长，加 `--wait 0` 立返 taskId，再用 `--status` 查进度、`--stop` 强杀。模板描述末尾 `[平均 X 秒]` 是实测耗时，直接据此判断；
  - 界面跳转后插一步 `wait`（500~1500ms）再操作下一步；
  - 回执超限（`resultTruncated:true`，此标记在**外层**、与 `result` 同级）→ 此时 `result` 是被截断的**残缺 JSON，不可 `JSON.parse`**（硬解析必报错），只能当纯文本预览读；全文在 `resultFile` 落盘路径，**严禁整段读进上下文**，用 grep 按行 offset 取片段；全文即交付物时反向问用户要摘要还是全文。
- **第 3 步 看屏幕**：按下节回退链逐级实测，别一上来就截屏。
- **第 4 步 终态一次性汇报**：一句话结果，或"卡在第 N 步 + 原因 + 手动补救建议"。**中间过程不打扰用户。**

## 看屏回退链（省 token 优先，逐级实测，不跳级）

1. **控件树**：`inspect_control_by_*` 取 `text`/`id`/`desc` — 最准最省。**禁止回传整棵 UI 树 JSON**。控件树不全 / APP 屏蔽无障碍 → 下一级。
2. **OCR**：`ocr` 模板取文字。文字多的屏带 `{"query":"关键词"}` 源头过滤，`detail:true` 仅在确需坐标时用。纯图形界面 / 文字不足以判断 → 下一级。
3. **截图 + 模型直读**：`curl -s http://localhost:9421/screenshot` 落 PC PNG 后读图。最贵，仅当前两级不行时用；已知控件包围盒时用 `crop-screenshot` 只裁该区域。模型不支持多模态则本层不可用 → 直接问用户。

> 截图角落彩色圆点是**连接状态悬浮球**（红未连/绿已连/蓝执行中）。截屏本身就是一次任务，故经中继拍的每张截图里它恒为蓝色，属正常，勿据此误判连接状态。

## 按需查阅（别一次全读）

| 场景                                             | 读什么                                               |
| ------------------------------------------------ | ---------------------------------------------------- |
| 传文件 / 部署工程 / 更新客户端 / 查任务单 / 自测 | `references/脚本与工具速查.md`                       |
| 路径报错 / 传文件落点 / 写新工程 / 找脚本位置    | `references/目录架构与路径约定.md`                   |
| 写或改任何手机端 JS（**必读**）                  | `references/AI_AutoJS编码强制规范.md`                |
| 写一次性脚本 / 收不到回执 / 建文件建目录的坑     | `references/现场脚本规范.md`                         |
| 新建或扩展模板                                   | `references/AI_新建模板手册.md`                      |
| 截图失败 / 卡在"立即开始"授权弹框                | `references/截图权限与弹框处理.md`                   |
| 手机连不上（含四项权限、取 IP）                  | `references/手机连接排障.md`                         |
| 中继没响应 / 手机超时连不上 / 保证中继活着       | `references/中继存活SOP.md`                          |
| 客户端改不动 / 假死 / 自更新                     | `references/自动更新手机客户端.md`                   |
| 多文件工程部署（含资源）                         | `references/部署真实工程.md`                         |
| 判断"某引擎是不是自己 / 是不是客户端" / 自保护 | `references/引擎_self_识别与isSelf判定.md` |
| 悬浮球行为 / 客户端职责                          | `references/手机端常驻客户端说明.md`                 |
| 手机端 API 用法（设备/控件/图片/OCR/http/文件）  | `references/` 下六篇 `autojs6_*` 与 `AutoJs6_*` 文档 |
| 查有多少模板（纯查询，不下发任务）               | `node scripts/scan-tasks.js --human`                 |
| AutoJS 写法参考                                  | `scripts/autojs代码参考例子/`                        |
| 自定义 UI 控件（自定义标签 / defineAttr / 生命周期钩子 / 状态持久化） | `references/AutoJS6自定义控件.md`  |
| 查 AutoJs6 内置图标名（`@drawable/ic_xxx`） | Grep 搜 `scripts/autojs代码参考例子/内置图标/内置图标列表.js`（955 个、每行一个，命中只回几行，**勿整篇读取**；命名规律 `ic_<语义>_<颜色>_<尺寸>`） |

## 省 token 三原则

1. **只传路径不传代码**：模板存 PC，下发只传 `path`+`args`；回执默认 `{ok:1}` / `{ok:0,err}`，不得要求回传整棵 UI 树或大段文本。
2. **全自动连跑**：中间步骤静默执行，终态一次性汇报。
3. **两级读取 + 2K 预算**：规划只读 description，选中才读正文；回执超 2000 字符自动截为预览 + 落盘路径。

## 高频误判点

- **把客户端当普通脚本停掉（最高频事故）**：失败清场时 `list-running-scripts` 会把执行端本体一并列出，它**看着就是个普通脚本**。识别：回执里 `isClient:true` / 在 `clientIds` 里 / `source` 是相对路径 `main.js` 且 `cwd` 在 `/data/user/0/<包名>/files/project`（路线A），或 `source` 为 `autojs-task-phone-client.js`（路线B）。**它不是可停目标**；`stop-script-by-id` 会拒停并回 `ok:0 + blockedClient`，此拒绝是护栏不是 bug。
- **熄屏/锁屏**：截全黑帧 + 剪贴板读空 + OCR 异常，而 `open-app` 却"假成功"（`am start` 不需亮屏）→ 让用户点亮解锁后重试，别往脚本 bug 方向排查。
- **setInterval 保活分场景**：为"等任务回执"而保活不要做——经 `/run` 下发的脚本由中继托管，正常 exit 即回执；但**悬浮窗/常驻 UI 类脚本必须 `setInterval(空函数, 3000)` 保活**，否则脚本引擎随主线程结束而退出，悬浮窗瞬间被销毁（用户约定的标准做法）。
- **UI 常驻任务关残留窗口，`--stop` 无效**：建好即回执后任务单已终态，`--stop` 返回 `alreadyFinished` 而引擎仍常驻、窗口还在 → 正确链路：`list-running-scripts` 拿实时引擎 id → `stop-script-by-id` 停掉（见 `references/引擎_self_识别与isSelf判定.md` §8）。
- **UI 树查找慢**：界面层级深时 `tap-text` 等会慢，属正常，等回执即可。
